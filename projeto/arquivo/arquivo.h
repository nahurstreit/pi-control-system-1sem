#include "../global/global.h"

extern void popularVetores();
extern void handleSalvar(int caso);

void criarLinhaArquivo(char stringDestino[], char stringAdicao[]);

extern bool checarLinhaNula(char token[]);

extern bool checarCampoNulo_Arquivo(char token[]);

extern bool checarCampoNulo_Vetor(char campo[]);

extern void salvarArquivo_Itens(int numCadCliente, int numOrdemProducao);
